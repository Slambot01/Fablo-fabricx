# Generated by Fablo (poc/fabric-x-integration)
# FSC core.yaml for endorser1
#
# IMPORTANT: This is NOT a standard Fabric core.yaml
# This is a Fabric Smart Client (FSC) configuration file.
# Standard CORE_PEER_* environment variables do NOT apply here.
# Schema source: fabric-x/samples/tokens/conf/endorser1/core.yaml

logging:
  spec: info
  format: "%{color}%{time:15:04:05.000} [%{module}] %{shortfunc} %{level:.4s}%{color:reset} %{message}"

fsc:
  id: endorser1                          # CORE_FSC_ID
  identity:
    cert:
      file: ./keys/node.crt
    key:
      file: ./keys/node.key
  p2p:
    listenAddress: /ip4/0.0.0.0/tcp/9301  # CORE_P2P_LISTENADDRESS
    type: websocket
    opts:
      routing:
        path: ./routing-config.yaml
  persistences:
    default:
      type: sqlite
      opts:
        dataSource: file:./data/fts.sqlite  # CORE_FSC_PERSISTENCES_OPTS_DATASOURCE
  endpoint:
    resolvers:
      - name: endorser1
        identity:
          id: endorser1
          path: ./keys/nodes/endorser1.crt
        addresses:
          P2P: endorser1.example.com:9301
      - name: endorser2
        identity:
          id: endorser2
          path: ./keys/nodes/endorser2.crt
        addresses:
          P2P: endorser2.example.com:9401

fabric:
  enabled: true
  default:
    driver: fabricx
    msps:
      - id: endorser
        mspType: bccsp
        mspID: Org1MSP
        path: ./keys/fabric/endorser
    tls:
      enabled: false
    peers:
      - address: committer-sidecar:4001
    queryService:
      - address: committer-queryservice:7001
    channels:
      - name: mychannel
        default: true
